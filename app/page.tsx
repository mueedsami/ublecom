'use client'
import Header from '@/components/Header';import KpiCard from '@/components/KpiCard';import AccountGauges from '@/components/AccountGauges';import Heatmap from '@/components/Heatmap';import Trend from '@/components/Trend';import Loading from '@/components/Loading';import {useOverview} from '@/lib/useOverview'
export default function Page(){const {data:d,error,loading}=useOverview();if(loading||error||!d)return <><Header eyebrow="Digital Shelf Command Center" title="Today at a glance" subtitle="Availability, price compliance and search visibility in one manager view."/><Loading error={error}/></>;const total=d.accounts.reduce((a:any,r:any)=>a+Number(r.scoped_basepacks||0),0);const yes=d.accounts.reduce((a:any,r:any)=>a+(Number(r.scoped_basepacks||0)-Number(r.nola_count||0)),0);const overall=total?100*yes/total:0;const nolaTotal=d.accounts.reduce((a:any,r:any)=>a+Number(r.nola_count||0),0);const cppActive=d.cpp.filter((x:any)=>x.compliant!==null&&x.compliant!==undefined);const cppPct=cppActive.length?100*cppActive.filter((x:any)=>x.compliant).length/cppActive.length:0;const sosFound=d.sos.length?100*d.sos.filter((x:any)=>x.found).length/d.sos.length:0;return <><Header eyebrow="Digital Shelf Command Center" title="Today at a glance" subtitle="Availability, price compliance and search visibility in one manager view."/><div className="grid kpis"><KpiCard label="Overall OLA" value={`${overall.toFixed(1)}%`} note={`${total} active account/location scopes`} tone="green"/><KpiCard label="NOLA today" value={nolaTotal} note={`${nolaTotal} total gaps · priority list below`} tone={nolaTotal?'red':'green'}/><KpiCard label="CPP compliance" value={`${cppPct.toFixed(1)}%`} note="Latest live campaign checks" tone={cppPct>=90?'green':'amber'}/><KpiCard label="SoS target visibility" value={`${sosFound.toFixed(1)}%`} note="Latest captured target positions" tone={sosFound>=80?'green':'amber'}/></div><div className="grid two"><div className="card"><div className="section-title"><h2>Retailer availability</h2><span>latest snapshot</span></div><AccountGauges rows={d.accounts}/></div><div className="card"><div className="section-title"><h2>Data freshness</h2><span>last successful observation</span></div>{d.freshness.map((x:any)=><div className="metric-row" key={x.account_name}><span>{x.account_name}</span><span style={{color:'var(--muted)',fontSize:11}}>{x.last_observed_at?new Date(x.last_observed_at).toLocaleString():'No data'}</span></div>)}</div></div><div className="grid two" style={{marginTop:16}}><div className="card"><div className="section-title"><h2>OLA trend</h2><span>daily portfolio average</span></div><Trend data={d.trend}/></div><div className="card"><div className="section-title"><h2>Priority NOLA</h2><span>latest snapshot</span></div><div className="table-wrap"><table className="table"><thead><tr><th>Account</th><th>Location</th><th>Basepack</th><th>Brand</th></tr></thead><tbody>{d.nola.slice(0,8).map((r:any,i:number)=><tr key={i}><td>{r.account_name}</td><td>{r.location_name||'All'}</td><td>{r.basepack}</td><td>{r.brand}</td></tr>)}</tbody></table></div></div></div><div className="card" style={{marginTop:16}}><div className="section-title"><h2>Brand × retailer availability</h2><span>red &lt; 80 · amber 80–89 · green ≥ 90</span></div><Heatmap rows={d.heat} accounts={d.accounts}/></div></>}
