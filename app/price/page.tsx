'use client'
import Header from '@/components/Header';import KpiCard from '@/components/KpiCard';import Loading from '@/components/Loading';import {useOverview} from '@/lib/useOverview'
export default function Page(){const {data:d,error,loading}=useOverview();if(loading||error||!d)return <><Header eyebrow="Online Price Analytics" title="CPP compliance without manual digging" subtitle="Compare the expected CPP price with the latest shopper-visible selling price."/><Loading error={error}/></>;const checked=d.cpp.filter((x:any)=>x.compliant!==null&&x.compliant!==undefined);const good=checked.filter((x:any)=>x.compliant).length;const bad=checked.length-good;return <><Header eyebrow="Online Price Analytics" title="CPP compliance without manual digging" subtitle="Compare the expected CPP price with the latest shopper-visible selling price."/><div className="grid kpis"><KpiCard label="CPP compliance" value={`${checked.length?(100*good/checked.length).toFixed(1):'0'}%`} tone="green"/><KpiCard label="Compliant" value={good} tone="green"/><KpiCard label="Price mismatch" value={bad} tone={bad?'red':'green'}/><KpiCard label="Checked items" value={checked.length}/></div><div className="card"><div className="section-title"><h2>Live campaign compliance</h2><span>latest check per CPP item</span></div><div className="table-wrap"><table className="table"><thead><tr><th>Campaign</th><th>Account</th><th>Basepack</th><th>Brand</th><th>CPP</th><th>Live</th><th>Status</th></tr></thead><tbody>{d.cpp.map((r:any,i:number)=><tr key={i}><td>{r.campaign_name}</td><td>{r.account_name}</td><td>{r.basepack}</td><td>{r.brand}</td><td>৳{r.expected_price}</td><td>{r.observed_price==null?'—':`৳${r.observed_price}`}</td><td><span className={'status '+(r.compliant?'ok':'bad')}><i className="dot"/>{r.status}</span></td></tr>)}</tbody></table></div></div></>}
