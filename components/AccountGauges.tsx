export default function AccountGauges({rows}:{rows:any[]}){return <div className="gauge-grid">{rows.map(r=><div className="gauge" key={r.account_code||r.account_name}><div className="gauge-top"><span>{r.account_name}</span><b>{Number(r.ola_pct).toFixed(1)}%</b></div><div className="bar"><i style={{width:`${Math.max(0,Math.min(100,Number(r.ola_pct)))}%`}}/></div><div className="kpi-delta">{r.nola_count} NOLA · {r.scoped_basepacks} scopes</div></div>)}</div>}
